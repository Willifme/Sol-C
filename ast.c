#include "ast.h"
/*
Node *makeNode(Node *node) {

  // Malloc the Node
  Node *newNode = (Node *)malloc(sizeof(Node));

  newNode->node = node;
 
  return newNode;
  
}
*/
Int makeInt(int value) {

  Int node = {value};

  return node;
  
}


















